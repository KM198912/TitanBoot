# TitanBoot Configuration File
# Simple bootloader config for kernel and modules

# Kernel command line (optional)
# cmdline=root=/dev/sda1 quiet splash

# Modules to load (ramdisk, drivers, etc.)
# Format: module <start_sector> <num_sectors> <name>
# Modules are loaded after the kernel

# Example ramdisk module (loaded from disk sector 100, 50 sectors)
module 100 50 initrd.img

# Example driver module
# module 200 20 driver.sys

# Currently 1 test module configured - add your modules above
